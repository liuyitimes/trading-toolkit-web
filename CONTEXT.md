# Trading Toolkit Context

## Glossary

### LOF 交割时效

一只 LOF 从申购提交到份额确认，以及从赎回提交到资金到账所需的基金业务日规则。申购确认和赎回到账分别以 `T+n` 表示；它们必须保留当前有效的基金管理人或发行人来源，未核验时显示为暂缺。

### LOF Published-NAV Premium

以同一交易日的场内收盘价和基金公司已公布单位净值计算的溢价率。它是已观测的日度指标，不等同于盘中估算或可执行套利。

### LOF Holdings-Implied Premium

以基金最近一次披露的组合持仓和盘中证券价格推算的实时溢价估计。它必须显示持仓披露日期、估算时间和覆盖限制，不得表示为基金公司公布净值或确认收益。

### LOF Portfolio Holdings

基金公司披露的基金投资组合及其权重、披露日期和来源。它不是用户的持仓，也不是基金份额或申赎变化。

### 名言弹幕（Quote Barrage）

从用户维护的名言列表中随机抽取、横向飘过应用界面的非阻断式展示。它服务于浏览氛围，不得遮挡或妨碍任何页面的主要内容与操作。

### 配债候选标的（Placement Candidate）

尚未上市且已进入上市委通过、同意注册或后续登记、申购、待上市阶段的可转债原股东配售观察项。登记日今天或未来的项可观察，登记日未知的项保留待跟踪，登记日已过的项仍保留作复盘并标记为已过期。候选标的包含发行条款、登记时间、配债来源信息和派生决策指标。

### 配债详情

单个配债候选标的可导出的决策记录。它包含候选标的完整配债详情字段、用户发起导出时生效的预期上市溢价假设及派生指标、逐候选标的配债来源信息和公告链接、可用时的快照新鲜度与发行条款核验或复核状态、1 至 5 手成本表，以及配债是规划观察而非确认收益的固定提示。

### 配债来源信息

配债详情导出文档中的逐候选标的来源依据：参与资格、登记日、配售条款、缴款时点、公告日期、公告 URL、核验时间和需复核状态。跨端唯一字段名为 `placement_provenance`；缺失字段必须明确显示为不可用，不得推断。`placement_evidence` 和裸 `provenance` 是不再支持的历史别名。

### 配债详情导出文档

只包含一个配债候选标的配债详情的 Markdown 文件。Web 从配债视图已加载的候选标的创建它。用户从该候选标的桌面表格行或移动端卡片直接发起下载，无需确认弹窗；配债视图不提供批量下载。未核验或需复核候选标的仍可导出，文档会明确其状态。

### 配债详情导出文件名

配债详情导出文档的文件名：`配债详情-{正股名称}（{正股代码}）-{导出日期}.md`。导出清洗只能替换用户文件系统不允许的字符。

### 配债快照（Placement Snapshot）

带时间戳的本地持久化配债候选标的副本，用于保证即时可用。它不是对当前市场事实的永久断言，只记录数据上次已知时的来源和核验时间。

### 配债观察记录（Placement Observation）

配债候选标的或其来源字段的历史版本。观察记录保留发行条款、日期和配债来源信息的变更，供审计和后续分析使用。

### 延迟配债快照（Stale Placement Snapshot）

刷新目标已过期或上次刷新失败的配债快照。只要候选标的仍与参与资格相关，它就保持可见，并必须披露数据时间和延迟原因，而不是静默删除。

### 配债字段来源追溯信息（Placement Field Provenance）

附着在一组配债字段上的来源追溯信息。发行条款和股权登记日需要公告标识、URL、发布时间和核验时间；市场字段需要数据提供方和观测时间；派生指标需要计算版本和输入快照引用。它是领域概念，不是待发配债接口的字段别名。

### 配债刷新任务（Placement Refresh Job）

获取外部配债数据并将其协调进配债快照和配债观察记录的异步任务。面向用户的读取不等待该任务；强制刷新只请求或观察任务，而不是在 HTTP 请求中执行数据提供方 I/O。

### 配债刷新策略（Placement Refresh Policy）

配债刷新任务的调度策略：服务启动时执行；交易日 08:30 至 18:00 每 15 分钟执行一次，其他时间每两小时执行一次；登记日为今天或明天的候选标的每五分钟执行一次。同类刷新同一时刻只能运行一个，失败时按指数退避重试。

### 配债保留策略（Placement Retention Policy）

配债快照在登记日后保留 30 天。配债观察记录和公告元数据保留三年。每日清理会软删除过期的当前快照，但不会删除其审计历史。

### 配债来源优先级（Placement Source Priority）

配债字段来源追溯信息的冲突规则：交易所或巨潮资讯官方公告优先于发行人公告页，发行人公告页优先于东方财富发行列表，东方财富发行列表优先于其他市场数据或推断值。同优先级冲突必须复核；可审计的人工确认可覆盖自动来源。

### 配债新鲜度状态（Placement Freshness State）

配债快照的用户可见状态。最新数据展示更新时间；延迟快照展示上次成功时间和原因；已核验发行条款标识公告核验；同优先级冲突显示为需要复核。即将登记的候选标的无论市场字段新鲜度如何都保持可见。

### 可转债股权登记日收盘价

可转债原股东配售对应正股在股权登记日的可验证收盘价。它是配债详情中安全垫、参与后收益和收益率的固定基准，不得显示为正股当前价。

### 配债参与后总收益

配债详情中用于观察参与配债结果的总收益指标，合并正股从股权登记日收盘后到后续收尾价的盈亏，以及可转债按当前预期上市溢价假设计算的收益。它是观察指标，不是确认成交收益。

### 今年上市新债列表

可转债工作区中当前自然年已上市新债的观察列表，用于比较发行以来涨幅、发行规模与换手率。已上市时间不足或历史价格缺失时展示不可用，不以零或当前价伪造历史表现。

### 新债发行规模

今年上市新债列表中的发行规模字段，直接取东方财富发行规模（亿元）。它是观察指标，不代表实时流通规模、锁定份额已扣除的规模或转股后的余额。

### 新债换手率

今年上市新债列表中的当日换手率（%），直接取东方财富行情接口的换手率字段。缺失时展示不可用，不得以零值伪装。

### 配债首日可交易量

服务端仅在巨潮公告核验原股东优先配售合计后返回的发行规模扣减值。客户端不得把 `null` 归一化为 0 或发行规模；不可用状态显示 `--` 并保留缺失原因。

### 配债公告数据状态

`verified` 表示公告解析值，`unavailable` 表示公告未发布、未找到或字段无法解析。列表、详情和公式提示必须保留该状态语义。

### LOF 连续正溢价

同一 LOF 在连续交易日中，场内价格相对该交易日已公布单位净值严格为正的已观测序列长度。仅在价格和净值均有效时产生观测；自然日、盘中估值及缺失观测不计入该序列。它是历史观察指标，不表示可执行套利机会或确认收益。

### LOF 连续正溢价状态

连续正溢价计算结果的可信度标识。`complete` 表示当前连续序列所需的交易日观测完整；`partial` 表示当前交易日已观测为正溢价，但缺少足以证明更早连续性的历史记录。`partial` 的天数是已观测下界，界面必须同时披露“历史不足”。

### LOF 溢价持续性

以 `consecutive_positive_sessions`、`status`、`as_of`、`history_started_on` 和 `reason` 描述的连续正溢价结果。它同时表达已观测天数和其历史可信度；不可用或历史不足不能压缩为数值零。

在列表中，`complete` 显示为“N 天”，`partial` 显示为“至少 N 天”并披露“历史不足”，`unavailable` 显示为“--”及原因。当前交易日同日可比但溢价不为正时，连续正溢价为 `0` 且状态为 `complete`；该零是业务事实，不得与缺失观测混用。
