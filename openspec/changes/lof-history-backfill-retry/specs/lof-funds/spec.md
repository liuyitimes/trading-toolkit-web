## MODIFIED Requirements

### Requirement: 溢价数据来源

LOF 页面 MUST 展示服务端提供的连续溢价状态，不得将历史回补未覆盖、同日净值尚未发布或申赎路径缺少审计证据的情况展示为零值或可执行 T+n。

#### Scenario: 回补后存在连续正溢价事实

- **假定**：服务端返回 `complete` 或 `partial` 的连续正溢价。
- **当**：用户查看 LOF 列表。
- **则**：页面显示实际天数或“至少 N 天”。
- **并且**：`partial` 同时保留历史不足提示。
