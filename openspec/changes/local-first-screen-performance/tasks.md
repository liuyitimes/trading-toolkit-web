# 任务

- [x] 1. 修正本机浏览器 API 基地址，使开发和预览经由相对 `/api` 代理。
  - 依赖：服务端同名变更提供本地快速读取路径。
  - 验证：浏览器网络请求不再指向 Render 远端。
- [x] 2. 调整 LOF 与可转债加载编排，分离首屏与附属指标。
  - 依赖：任务 1。
  - 验证：模拟 2.5 秒慢摘要/信号时，首屏在 1 秒内解除加载。
- [x] 3. 运行浏览器回归、构建及 OpenSpec 验证。
  - 依赖：任务 1、2 和服务端验收。
  - 验证：执行性能回归、`npm run build` 和 `openspec validate`。
