# 提案：本地首屏性能与 API 路由

## 背景

本地 `vite preview` 读取生产环境远端 API 配置，导致浏览器跨域失败；LOF 页面还会等待摘要接口、可转债页面会等待信号接口才结束加载，使局部慢接口拖住整页。

## 目标

- 本机访问的 Web 运行时通过相对 `/api` 使用 Vite 代理访问 Ubuntu 后端。
- LOF 主列表返回后立即呈现，摘要异步补齐。
- 可转债不等待慢信号接口才解除首屏加载。

## 配套变更

服务端仓库存在同名变更，负责 1 秒内返回缓存、快照或预热状态。
