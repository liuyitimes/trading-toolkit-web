## 背景

配债页面当前会过滤登记日已过的候选项，用户无法观察近期已完成登记、尚未上市的标的，也无法按临近登记日进行稳定比较。

## 变更内容

- 页面保留服务端返回的全部观察项，不再进行客户端资格过滤。
- 按 `eligible`、`registration_unknown`、`expired` 三组排序；组内分别按登记日升序、原顺序、登记日降序。
- 已过登记日的行和详情显示“已过期”标记。
- 与服务仓库使用同名 `placement-observation-total-return` 变更，兼容旧响应并在缺少状态字段时回退到登记日判断。
