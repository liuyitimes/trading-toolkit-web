## 目标

让配债列表首先呈现临近登记日的可观察项，同时保留历史观察项供复盘。

## 决策

Store 负责将 API 行规范化并执行稳定三组排序；视图只展示 `regBadge` 和详情状态。服务端状态字段优先，旧数组响应缺少字段时由规范化函数生成兼容状态。排序不会改变过滤子标签的语义，也不把过期项移出“全部”列表。

## 非目标

本变更不计算登记日收盘价、后续交易日收盘价或总收益；这些字段由后续依赖任务增加。
