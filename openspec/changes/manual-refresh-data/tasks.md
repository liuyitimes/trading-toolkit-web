# 任务

## 1. 规范与共享刷新编排

- [x] 1.1 在应用 store 中增加当前路由刷新回调注册、清理、加载态和成功/失败状态。
- [x] 1.2 在共享布局顶部加入带 tooltip、无障碍名称和加载态的刷新按钮。
- [x] 1.3 为手动刷新术语更新领域上下文文档。

## 2. 页面与 API 接入

- [x] 2.1 让首页、四个业务列表页和三个详情页注册当前页面刷新动作。
- [x] 2.2 为业务 API 和 store 增加可选 `refresh` 参数，保持默认加载行为不变。
- [x] 2.3 确认刷新成功只更新共享更新时间，失败保留已有数据和旧时间。

## 3. 服务端契约与验证

- [x] 3.1 让综合市场概览路由读取并传递 `refresh=true`，并建立服务端配套 OpenSpec 变更。
- [x] 3.2 增加 API 查询参数和刷新编排的聚焦验证。
- [x] 3.3 运行 Web/Service 构建、OpenSpec 校验，并检查桌面端和移动端刷新按钮布局与交互。
