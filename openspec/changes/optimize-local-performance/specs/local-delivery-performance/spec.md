## ADDED Requirements

### Requirement: 首页独立数据并发加载

Web MUST（必须）并发请求首页互不依赖的概览和情绪数据，不得使一个请求的完成成为另一个请求的开始条件。

#### Scenario: 情绪接口较慢

- **WHEN** 首页概览接口先返回而情绪接口仍在加载
- **THEN** 概览卡片立即显示可用数据
- **AND** 页面继续等待情绪结果而不阻塞已完成内容
