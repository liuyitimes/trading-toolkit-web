# 任务：全站名言弹幕

- [x] 新增名言 store，将名言 CRUD 与 `quoteManager` 的本地持久化协调为响应式状态。
- [x] 重构名言管理页使用名言 store，并验证增、改、删、恢复默认的现有行为不变。
- [x] 重构弹幕组件以支持随机批次、40 秒播放、4 秒间隔和延迟应用列表更新。
- [x] 将弹幕安装到桌面端共享布局的页头与内容区之间，并接入图标关闭按钮和路由批次刷新。
- [x] 为窄屏与减少动态效果添加隐藏规则，确认普通弹幕区域点击穿透。
- [x] 构建 Web 应用，并在桌面端和移动端检查布局、关闭操作、路由切换、名言更新及无控制台错误。
- [x] 运行 `openspec validate global-quote-barrage --json`。
