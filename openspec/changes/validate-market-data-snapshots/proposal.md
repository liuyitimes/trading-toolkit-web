## 背景与目标

服务端将拒绝不完整可转债记录和不合格恢复快照。本配套变更记录 Web 继续消费不变的列表契约，避免将缺失转股字段的行情当作可用决策数据展示。

## 范围与非目标

- 维持现有可转债列表 API 消费方式。
- 不新增前端推断、补值或替代数据。
- 本次不修改路由、页面布局或 API 路径。

## 影响

配套 Service 变更为 `trading-toolkit-service/validate-market-data-snapshots`。HTTP 响应信封和字段名称保持兼容。
