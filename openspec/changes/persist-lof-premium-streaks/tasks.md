## 1. 数据归一化

- [x] 1.1 将 `premium_persistence` 归一化为 LOF 视图模型，保留完整、历史不足和不可用状态。
- [x] 1.2 为真实零天、历史不足和不可用状态添加聚焦 Store 测试。

## 2. 页面展示

- [x] 2.1 更新桌面表格、移动卡片和详情区域的连续正溢价展示与原因提示。
- [x] 2.2 确保页面不再将缺失 API 字段展示为 `0 天`。

## 3. 验证

- [x] 3.1 运行构建与 LOF 浏览器验证，覆盖完整、历史不足、不可用和真实零天状态。
- [x] 3.2 运行 OpenSpec 校验并与配套 Service 契约核对。
