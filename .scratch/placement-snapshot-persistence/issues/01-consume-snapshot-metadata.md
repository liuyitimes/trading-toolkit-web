# 01 - Consume Placement Snapshot Metadata

Type: feature
Status: resolved
Labels: feature, api-contract

Render snapshot freshness, stale reason, verification state, and review-required state when the service provides them. Keep legacy list payload compatibility.

## Answer

The store accepts legacy arrays and the new snapshot payload. The placement toolbar displays freshness, stale reason, verified issuer terms, and source-conflict review state when metadata is available.
